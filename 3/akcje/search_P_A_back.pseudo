search_P_A_back()
{

    set grid points 
    wyznacz trajektorie rak
    FLOODING_ALGO_BACK()
   
    SELECT_OPTIMAL_PATH_B()
    if(path_found=False)
    {
        
        SELECT_TEMPORARY_PATH()//to chyba było w move along
    
    }
}